Входными данными для обратной задачи являются выходные данные прямой задачи.
Задаем какую-то трехмерную структуру ячеек, задаем для них параметры намагниченности p и вычисляем по формуле расчета Bx, By, Bz

Если в варианте учитывается только одна компонента направленности, то остальные рассчитывать не надо.
В обратной задаче тогда при составлении матрицы L блоки будут размером не 3х3, а 1х3

Ячейки можно представить как одномерный вектор с координатами, а не матрицу, чтобы упростить задачу
Далее в обратной задаче вычисляем матрицу L, её элементы - слагаемые в формулах Bx, By, Bz

Вопросы:
1. Как лучше задавать сетку? Минимальным-максимальным значением и числом ячеек или каждую ячейку по отдельности?
Ответ: как удобнее. Но поскольку нужно будет тестировать на сетках большого размера, то удобнее будет сделать генератор. Мб сделать генератор,
который будет формировать .dat файл со всеми ячейками по отдельности
2. Матрица L составляется для каждой пары ячейка-приемник?
Ответ: да
3. Цвет ячейки в сетке отображает только одну компоненту направленности (px). Что делать если py и pz ненулевые?
Ответ: строить сетку под каждую компоненту направленности


Проблемы:
1. По какой-то причине если длина ячейки становится сильно больше чем её высота, то компонента намагниченности выглядит херово. Возможно, потому что считается её центр